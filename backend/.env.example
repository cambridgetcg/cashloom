FRONTEND_ORIGIN=
MONGO_URI=
# Required in production — a strong random secret. Boot fails if left unset there.
JWT_SECRET=
GEMINI_API_KEY=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
RESEND_API_KEY=
# Verified "from" address for emails (reports, password reset). Without it,
# the from address is invalid and emails won't send.
RESEND_MAILER_SENDER=
