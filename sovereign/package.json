{
  "name": "cashloom-sovereign",
  "version": "0.1.0",
  "description": "CashLoom sovereign node — non-custodial, local-first. Everyone pays everyone. One process, one file, your machine.",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch src/index.ts",
    "build:ui": "cd ui && bun install && bun run build",
    "test": "bunx vitest run src/connectors src/utils && bun test src/vault.test.ts src/sync.test.ts src/pay.test.ts src/senders/btc.sender.test.ts",
    "test:connectors": "bunx vitest run src/connectors src/utils",
    "test:node": "bun test src/vault.test.ts src/sync.test.ts src/pay.test.ts src/senders/btc.sender.test.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "~2.2.0",
    "@noble/hashes": "^2.0.1",
    "@scure/base": "~2.2.0",
    "@scure/btc-signer": "^2.2.0",
    "axios": "^1.7.9",
    "hono": "^4.12.0",
    "viem": "^2.21.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "@types/bun": "^1.3.0"
  }
}
